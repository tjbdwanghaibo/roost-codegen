# Roost 项目生成器使用说明

## 1. 目标

roost-codegen 用一份 roost.yaml 管理项目结构、Service、Kit Mod、代码生成、基础配置、本地依赖和 CI。业务层只实现 Service、Controller、Entity 等业务逻辑，不负责重复的 App 装配、Mod 生命周期和生成命令维护。

生成器遵循以下边界：

- 带有 Code generated by roost-codegen 的文件由 project sync 管理。
- Service、配置、main.go 等业务可编辑文件只在缺失时创建。
- 新项目先写入同级临时目录，全部生成成功后才原子改名。
- 单个文件使用临时文件、Flush 和 Rename 更新。
- 不生成 .gitkeep；需要保留包时生成有效 doc.go。

## 2. 安装和运行

推荐直接运行固定发布版本，不需要把 codegen 加进业务 module：

    go run github.com/tjbdwanghaibo/roost-codegen/cmd/roost@v1.4.0 help

也可以安装本地命令：

    go install github.com/tjbdwanghaibo/roost-codegen/cmd/roost@v1.4.0
    roost help

生产项目不要在 Makefile 或 CI 中使用 @latest。创建项目时可以查询最新版本，但落盘后必须固定版本。

### Windows 安装与 PATH

在 roost-codegen 仓库中可以运行可重复执行的安装脚本。脚本读取当前
`GOBIN`/`GOPATH`，保留它们和现有用户 PATH，只追加 `go install` 实际使用的
Go bin 目录：

    powershell -ExecutionPolicy Bypass -File .\scripts\install-windows.ps1

手工配置的等价步骤如下：

    $goBin = (go env GOBIN).Trim()
    if (-not $goBin) {
        $goPath = ((go env GOPATH).Trim() -split [IO.Path]::PathSeparator)[0]
        $goBin = Join-Path $goPath "bin"
    }
    $userPath = [Environment]::GetEnvironmentVariable("Path", "User")
    $entries = @($userPath -split ";" | Where-Object { -not [string]::IsNullOrWhiteSpace($_) })
    if (-not ($entries | Where-Object { $_.Trim().TrimEnd("\") -ieq $goBin.TrimEnd("\") })) {
        [Environment]::SetEnvironmentVariable("Path", (($entries + $goBin) -join ";"), "User")
    }

例如当前 `GOPATH` 为 `D:\Program Files\Go\bin` 且 `GOBIN` 为空时，需要加入
PATH 的目录就是 `D:\Program Files\Go\bin\bin`。关闭并重新打开 PowerShell 后执行：

    go install github.com/tjbdwanghaibo/roost-codegen/cmd/roost@v1.4.0
    Get-Command roost
    roost help

如果安装成功但找不到命令，先检查：

    go env GOBIN GOPATH
    $goBin = go env GOBIN
    if (-not $goBin) { $goBin = Join-Path (go env GOPATH) "bin" }
    Get-ChildItem $goBin -Filter roost.exe
    $env:Path -split ";"

用户 PATH 修改后，已经打开的 PowerShell 不会自动刷新。可以重新打开终端，或只在当前窗口临时执行：

    $goBin = (go env GOBIN).Trim()
    if (-not $goBin) { $goBin = Join-Path (((go env GOPATH).Trim() -split [IO.Path]::PathSeparator)[0]) "bin" }
    $env:Path += ";$goBin"

## 3. 创建项目

最小命令：

    roost project new planet

指定 module、服务和能力：

    roost project new planet \
      -module github.com/acme/planet \
      -services game,gate \
      -mods configdata,etcd,redis,mongo,nats,sync,remote_entity \
      -features protocol,config,entity,nest,event,dao \
      -out D:/work/planet

固定依赖版本：

    roost project new planet \
      -roost-core-version v1.4.0 \
      -roost-kit-version v1.4.0 \
      -roost-skill-version v1.0.0 \
      -codegen-version v1.4.0

默认生成：

    planet/
      roost.yaml
      main.go
      Makefile
      Dockerfile
      internal/bootstrap/generated.go
      internal/service/game/service.go
      configs/service/
      configs/schema/
      configs/generated/
      configs/data/
      protocol/def/
      game/
      event/def/
      db/def/
      deploy/dev/docker-compose.yaml
      .github/workflows/ci.yml

## 4. roost.yaml

示例：

    schema: 1
    project:
      name: planet
      module: github.com/acme/planet
    versions:
      core: v1.4.0
      kit: v1.4.0
      skill: v1.0.0
      codegen: v1.4.0
    shared_mods:
      - lock
      - ops
      - statslog
    services:
      game:
        mods:
          - configdata
          - etcd
          - redis
          - mongo
          - nats
          - sync
          - remote_entity
      gate:
        mods:
          - etcd
    features:
      - protocol
      - config
      - entity
      - nest
      - event
      - dao

修改清单后执行：

    make sync
    make doctor

sync 会重新生成 App 装配、Makefile、CI、Docker Compose 和使用说明，不覆盖已经存在的 Service 实现与业务配置。

## 5. Kit Mod 自动装配

支持的 Mod：

- lock：进程内实体锁管理。
- ops：health、ready、metrics 和 admin HTTP。
- statslog：运行时、Entity 和 Nest 周期统计。
- configdata：不可变配置快照加载和热更。
- etcd：注册发现、watch、选主和本地镜像。
- redis：Redis、pipeline、pub/sub 和分布式锁。
- mongo：Mongo client、collection、session 和索引策略。
- nats：NATS、JetStream、RPC 和 Bus。
- sync：NATS 或 JetStream 同步 Bus；自动补充 nats。
- remote_entity：远程 Entity、版本锁和同步重试；自动补充 redis。

Core 会根据 DependsOn 排序生命周期。生成器负责补齐必需依赖，不复制 Kit 内部的连接、重试、health 或 shutdown 逻辑。

增加 Service：

    roost add service chat -mods etcd,redis,mongo,nats

给已有 Service 增加 Mod：

    roost add mod sync -service chat

## 6. 日常业务脚手架

    roost add module alliance
    roost add protocol AllianceCreate -group game
    roost add entity Player
    roost add component Inventory
    roost add event PlayerLogin
    roost add table Item
    roost add dao Player
    roost add webroute AdminQuery
    roost add errcode InvalidRequest
    roost add mod saga -service game
    roost add saga AllianceRally -service game -steps ReserveTroops,CreateMarch,CreateRally

Saga 命令会自动给目标 Service 添加 `saga`、`nestwal` 及其传递依赖，并生成
`saga/alliance_rally/definition.go`，其中包含类型、步骤、重试、
补偿 topic、`Register`、`EmitStart` 和幂等 `Start` 入口。业务 Nest handler 必须调用
`EmitStart`，使 Entity mutation 与 Saga 启动意图进入同一个 WAL record；`Start` 仅用于
durable consumer、管理和恢复路径。生成的 bootstrap 会把 manifest 中的
定义传给 Saga Mod；自定义装配时才需要调用 `Register`。步骤消费者使用
`cube-kit/saga.SubscribeStep`，并把
每个 command 的 `IdempotencyKey` 作为本地预留/确认/释放操作的唯一键；不要以
投递次数或 `CommandID` 作为业务幂等键。

每个步骤还会生成 `Subscribe<Step>` 和 `Subscribe<Step>Compensation`，不同服务
只订阅自己拥有的步骤。调用者显式传入 stream 和 durable；同一逻辑服务的多个
副本应共享 durable，不同处理器不得复用 durable。

每个骨架还会生成 `Definitions()`。修改步骤顺序或补偿语义时递增 `Version`，把旧定义
保留在 `Definitions()` 中；bootstrap 会注册全部仍在运行的版本。通过
`Engine.List` 确认旧版本已无非终态记录后，才能删除旧定义。

生成配置把 coordinator/publisher 的 claim batch 分开，并限制 `max_ack_pending`、
Mongo 操作时间、NAK 指数退避和 stream 最大磁盘占用。生产集群应把 Saga 与 Nest
effect stream 的 `replicas` 调整为 3；`start_effect_durable` 在同一 NATS domain 内必须
按应用唯一、在同一应用的所有 coordinator 副本间保持一致。扩容优先增加固定 worker，
修改 batch/timeout 时必须满足生成器文档中的 lease 预算并重新压测。

protocol、entity、component 默认从 roost.yaml 的 ID 空间分配下一个空闲 ID，也可以使用 -id 显式指定。

Makefile 也提供了对应入口，例如：

    make new-protocol NAME=PlayerLogin GROUP=game
    make new-table NAME=Item
    make new-service NAME=chat MODS=etcd,redis,nats
    make new-saga NAME=AllianceRally SERVICE=game STEPS=ReserveTroops,CreateMarch,CreateRally

生成业务骨架后运行：

    make generate-changed
    make test

## 7. ID 管理

查询下一个 ID：

    roost id next protocol -group game
    roost id next entity
    roost id next component

检查重复与越界：

    roost id check
    make id-check

当前扫描 //cube:* 和 //roost:* 标记，历史项目不需要立刻迁移 marker。

## 8. 生成流水线

执行全部启用的生成器：

    make generate

只执行受 Git 变更影响的生成器：

    make generate-changed

查看计划：

    roost generate --dry-run

严格检查：

    make check-generated

check-generated 会复制项目到临时目录，在副本中执行全部生成器并比较生成文件，不修改当前工作区。

默认顺序为 DAO、Event、Protocol、Entity、Nest、Attribute、Config Template、Config Data、Config Go、Web Route，避免 Nest 或业务注册早于其输入生成。

## 9. Protocol

通用项目默认生成 proto、Go PB、msgid 和 manifest：

    make generate

独立调用：

    go run github.com/tjbdwanghaibo/roost-codegen/cmd/protocol@v1.4.0 \
      -def ./protocol/def \
      -bind "" \
      -handlers "" \
      -robot-protocol "" \
      -force

Cube 风格项目可以启用 player_bind、handler 和 robot registry 输出。生成器会从目标 go.mod 发现 module，不再硬编码 Cube 业务 module。

## 10. ConfigData

创建表：

    roost add table Item
    make generate

流水线会：

1. 扫描 configs/schema。
2. 生成 configs/table_template CSV。
3. 将 configs/table CSV 转换为 configs/data JSON。
4. 生成 configs/generated/gen_table_config.go。
5. 将生成的 registry 在 App 创建前注册到 Core DefaultRegistry。

空项目也会生成空 registry，因此首次创建后即可编译。

配置检查：

    roost config check -service game
    roost config check -file configs/service/config.game.prod.yaml -production

production 检查拒绝 CHANGE_ME、localhost、127.0.0.1 和 dev token。真实密码、证书、UDP/KCP 密钥不得提交到仓库。

## 11. 启动和本地依赖

根据选中的 Mod，docker-compose 只包含需要的 Redis、MongoDB、NATS 和 etcd：

    make dev-up
    make dev-logs
    make dev-down

启动服务：

    make run SERVICE=game SID=1001

等价命令：

    go run . game --sid 1001 --config configs/service/config.game.yaml

Core 已经通过 RegisterServer 自动创建 Cobra 子命令，业务层不需要重复编写启动命令。

## 12. 构建和 CI

    make fmt
    make vet
    make test
    make test-race
    make build VERSION=v1.4.0
    make ci

build 使用 ldflags 注入 Core buildinfo 的 Version、Commit、BuildTime 和 Dirty。

CI 默认执行格式化、vet、测试、race、生成一致性、配置和 ID 检查。

## 13. Doctor、Diff 和升级

检查项目：

    roost project doctor
    roost project doctor -strict=false
    roost project doctor -json

查看 sync 将修改的文件：

    roost project diff

升级固定版本：

    roost project upgrade \
      -core v1.4.0 \
      -kit v1.4.0 \
      -skill v1.0.1 \
      -codegen v1.4.0

升级只更新 roost.yaml 和生成器管理的文件。业务代码和业务配置保留。

## 14. 生产检查清单

- Makefile 和 CI 使用固定 codegen 版本。
- prod 配置无 localhost、CHANGE_ME 或开发 token。
- ops admin 默认关闭；开启时使用外部密钥管理。
- etcd advertise address 可被其他节点访问。
- NATS reliable 模式同时启用 Redis。
- QUIC 使用正式证书和固定 ALPN。
- UDP/KCP 接入层配置鉴权、密钥轮换、Cookie、限速和 DDoS 防护。
- 提交前执行 make ci。
- 部署前执行 race 测试和优雅停服演练。
- MongoDB 使用 replica set 或支持 transaction 的 sharded cluster；禁止 standalone。
- `/var/lib/roost/wal` 使用单写持久卷，并验证异常重启后 replay。
- JetStream effect consumer 使用 `nestwal.MongoEffectInbox`，receipt TTL 大于消息最大保留时间。
- 房间慢消费者淘汰、断线 session 清理、100 Entity/20 Hz 和 Redis/Mongo/NATS 故障注入均通过 staging 门禁。

## 15. 当前 module path 兼容

已发布的 roost-core、roost-kit、roost-skill 版本目前仍声明 cube-* module path。生成的 go.mod 使用 cube-* require 加精确 roost-* replace，以兼容已发布版本。

等三个依赖完成 module path 正式迁移后，project upgrade 可以更新模板并移除 replace。

## 16. 帧同步 Transport 预设

在 features 中启用 replication-quic、replication-kcp 或 replication-udp 后，sync 会生成 internal/transport/generated.go：

- replication-quic：QUIC DATAGRAM + Reliable Stream，并包装 AsyncTransport。
- replication-kcp：KCP 可靠流 + OOB datagram，并包装 AsyncTransport。
- replication-udp：安全 UDP datagram + 外部 ReliableSender 组合。

生成的只是 Transport 装配入口。监听器、TLS、登录票据、会话密钥和 BindSession 仍属于网关接入层，不能用开发默认值代替生产安全配置。

这些 feature 要求 roost-kit >= v1.0.5。已发布的 v1.0.4 replication 包存在编译问题，生成器会在清单校验阶段直接拒绝该组合，不能作为生产依赖。
